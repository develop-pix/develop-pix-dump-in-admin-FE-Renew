import { Box } from '@mui/material';
import { Sidebar } from '../../components';

export default function UserManagePage() {
  return (
    <Box sx={{ display: 'flex' }}>
      <Sidebar />
    </Box>
  );
}
